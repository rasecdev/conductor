# CLAUDE.md — AgendaBot

Bot headless de agendamento. Plano em `tasks/plan.md`.

## Gates de qualidade

- `bash scripts/check.sh` precisa passar antes de avançar de fase. Ele valida o
  plano de QA em `docs/qa/plano.md`.
